#libraries included
"""
para crear nuevo hilo

"""
import threading #lo mismo que pthread

#Global scope variables

#Data structures

#opens threads for consumers
def Consumers (threading.Thread):
  def __init__(self,threadID,si): #id de hilo y su conjunto si
    threading.Thread.__init__(self)

#Functions